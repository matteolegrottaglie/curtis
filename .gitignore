# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# Local user data: database, browser profile (LinkedIn session), token,
# screenshots, logs. This must NEVER reach git.
data/
browser-profile/
screenshots/
token
daemon.pid
terms-accepted
browser-hints.json
*.db
*.db-shm
*.db-wal
*.bak
*.bak-*

# Environment
.env
.env.local

# Real people's data used for local selector probing and imports.
# targets.json holds live LinkedIn profile URLs — third parties' personal data.
targets.json
*.csv
!test/**/*.csv

# Logs
*.log

# macOS
.DS_Store

# Claude Code: per-machine local settings and temporary worktrees
.claude/settings.local.json
.claude/worktrees/

# Working files generated by mcp-use
.mcp-use/
